[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION             = "20"
  NEXT_TELEMETRY_DISABLED  = "1"

# Plugin obrigatorio para Next.js no Netlify (App Router + SSR)
[[plugins]]
  package = "@netlify/plugin-nextjs"